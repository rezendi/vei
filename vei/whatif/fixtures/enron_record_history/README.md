# Enron Record History

This package holds dated public Enron record events that are used to widen the
saved historical timeline around key branch points.

The records are kept small and explicit. Each item points at a public source and
is tagged with a `source_family` so the saved canonical timeline can show where
the extra context came from.
